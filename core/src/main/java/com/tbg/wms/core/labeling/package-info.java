/**
 * Shared labeling workflow helpers used by both CLI and GUI paths.
 *
 * <p>This package exists to centralize small but important cross-cutting helpers, such as virtual
 * LPN construction and shipment relabeling, so those rules do not drift across modules.</p>
 */
package com.tbg.wms.core.labeling;
