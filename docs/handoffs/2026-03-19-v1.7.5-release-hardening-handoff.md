# 1.7.5 Release Hardening Handoff

Date: 2026-03-19

Branch: `patch/v1.7.5-tropicana-bootstrap-fix`

PR: https://github.com/notzune/wms-pallet-tag-system/pull/40

Latest pushed commit: `87b72cd` (`docs: fix imported release doc links`)

Update: 2026-03-21 validation on the same branch closed the remaining smoke gaps:

- Barcode Generator smoke coverage added
- GUI-to-backend coverage inventory documented
- repo smoke passed
- packaged smoke passed
- packaged smoke with `-IncludeInstallerScenarios` passed

## Current State

The branch contains the full `1.7.5` patch line to date:

- Tropicana bootstrap embedded-config fix
- Packaged app config resolution fix for SKU matrix files
- Rail `SYSTEM_DEFAULT` print path fix
- Release smoke harness for repo and packaged modes
- Release-only Tropicana bootstrap installer smoke coverage

Public release `v1.7.4` remains published, but its Tropicana bootstrap path is known-bad. `1.7.5` is intended to supersede it once remaining release-hardening work is finished and revalidated.

## Verified Commands

These passed on this branch during the 2026-03-21 release-hardening rerun in this workspace:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\tests\run-smoke-tests.tests.ps1
powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\tests\build-tropicana-installer.tests.ps1
powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\tests\install-tropicana-config.tests.ps1
powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\tests\build-bundle-config-seeding.tests.ps1
.\mvnw.cmd -q -pl cli -am "-Dtest=BarcodeCommandTest" "-Dsurefire.failIfNoSpecifiedTests=false" test
.\mvnw.cmd -q -pl gui -am "-Dtest=LabelWorkflowServiceTest,AdvancedPrintWorkflowServiceTest,GuiPrinterTargetSupportTest,WorkflowShortcutBinderTest" "-Dsurefire.failIfNoSpecifiedTests=false" test
powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\run-smoke-tests.ps1 -Mode repo -ConfigPath .\.env -ShipmentId 8000574009 -CarrierMoveId 243177 -TrainId JC03182026
powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\run-smoke-tests.ps1 -Mode packaged -TargetPath .\dist\wms-tags-portable -ConfigPath .\.env -ShipmentId 8000574009 -CarrierMoveId 243177 -TrainId JC03182026 -IncludeInstallerScenarios
```

Latest smoke reports:

- `out/smoke-repo-20260321-122513/smoke-report.txt`
- `out/smoke-packaged-20260321-123051/smoke-report.txt`

## Important Implementation Notes

- Installer smoke is isolated from any real local install by building a smoke-specific app identity:
  - `AppName = "WMS Pallet Tag System Smoke"`
  - `InstallDirName = "WMS-Pallet-Tag-System-Smoke"`
  - random `WinUpgradeUuid`
- `scripts/run-smoke-tests.ps1` supports `-IncludeInstallerScenarios` for release-only checks.
- `scripts/smoke/smoke-manifest.json` now includes `tropicana-bootstrap-install`.
- `scripts/install-wms-installer.ps1` supports `-InstallDir` and `-QuietInstall`.
- `scripts/build-jpackage-bundle.ps1` supports `AppName`, `InstallDirName`, and `WinUpgradeUuid`.

## Recommended Next Step

Release hardening is complete for the current `1.7.5` scope.

Next actions:

1. Review the latest smoke evidence.
2. Tag `v1.7.5`.
3. Trigger the release workflow.
4. Rebuild the final Tropicana installer from the tagged commit.

Current supporting docs:

- `docs/gui-backend-coverage-inventory.md`
- `docs/release-smoke-coverage-matrix.md`
- `docs/release-checklist.md`

Latest smoke evidence:

- `out/smoke-repo-20260321-122513/smoke-report.txt`
- `out/smoke-packaged-20260321-123051/smoke-report.txt`

## Resume Commands

To resume from the home workspace:

```powershell
cd C:\Users\zrash\OneDrive\Documents\GitHub\wms-pallet-tag-system
git fetch origin
git checkout patch/v1.7.5-tropicana-bootstrap-fix
git pull --ff-only
```
