# 1.7.5 Release Hardening Handoff

Date: 2026-03-19

Branch: `patch/v1.7.5-tropicana-bootstrap-fix`

PR: https://github.com/notzune/wms-pallet-tag-system/pull/40

Latest pushed commit: `0aaf264` (`feat: add tropicana installer smoke coverage`)

## Current State

The branch contains the full `1.7.5` patch line to date:

- Tropicana bootstrap embedded-config fix
- Packaged app config resolution fix for SKU matrix files
- Rail `SYSTEM_DEFAULT` print path fix
- Release smoke harness for repo and packaged modes
- Release-only Tropicana bootstrap installer smoke coverage

Public release `v1.7.4` remains published, but its Tropicana bootstrap path is known-bad. `1.7.5` is intended to supersede it once remaining release-hardening work is finished and revalidated.

## Verified Commands

These passed on this branch before the last push:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\tests\run-smoke-tests.tests.ps1
powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\tests\build-tropicana-installer.tests.ps1
powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\tests\install-tropicana-config.tests.ps1
.\mvnw.cmd -q -pl cli -am "-Dtest=RailPrintCommandTest" "-Dsurefire.failIfNoSpecifiedTests=false" test
.\mvnw.cmd -q -pl core "-Dtest=RailPrintServiceTest" test
powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\run-smoke-tests.ps1 -Mode repo -ConfigPath C:\Users\zrashed\Documents\Code\wms-pallet-tag-system\.env -ShipmentId 8000574009 -CarrierMoveId 243177 -TrainId JC03182026
powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\run-smoke-tests.ps1 -Mode packaged -ConfigPath C:\Users\zrashed\Documents\Code\wms-pallet-tag-system\.env -ShipmentId 8000574009 -CarrierMoveId 243177 -TrainId JC03182026
powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\run-smoke-tests.ps1 -Mode packaged -ConfigPath C:\Users\zrashed\Documents\Code\wms-pallet-tag-system\.env -ShipmentId 8000574009 -CarrierMoveId 243177 -TrainId JC03182026 -IncludeInstallerScenarios
```

Installer-enabled smoke report:

- `out/smoke-packaged-20260319-215038/smoke-report.txt`

## Important Implementation Notes

- Installer smoke is isolated from any real local install by building a smoke-specific app identity:
  - `AppName = "WMS Pallet Tag System Smoke"`
  - `InstallDirName = "WMS-Pallet-Tag-System-Smoke"`
  - random `WinUpgradeUuid`
- `scripts/run-smoke-tests.ps1` supports `-IncludeInstallerScenarios` for release-only checks.
- `scripts/smoke/smoke-manifest.json` now includes `tropicana-bootstrap-install`.
- `scripts/install-wms-installer.ps1` supports `-InstallDir` and `-QuietInstall`.
- `scripts/build-jpackage-bundle.ps1` supports `AppName`, `InstallDirName`, and `WinUpgradeUuid`.

## Recommended Next Step

Close the remaining smoke coverage gaps before tagging `v1.7.5`.

Highest-value next slice:

1. Add smoke coverage for the `Barcode Generator` workflow.
2. Build a GUI-to-backend coverage inventory for any remaining manually exercised options.
3. Rerun the full release smoke set, including `-IncludeInstallerScenarios`.
4. If that passes, tag `v1.7.5`, trigger release, and rebuild the final Tropicana installer from the tagged commit.

## Resume Commands

To resume from the home workspace:

```powershell
cd C:\Users\zrashed\Documents\Code\wms-pallet-tag-system
git fetch origin
git checkout patch/v1.7.5-tropicana-bootstrap-fix
git pull --ff-only
```

If you want to work directly in the isolated worktree instead:

```powershell
cd C:\Users\zrashed\Documents\Code\wms-pallet-tag-system\.worktrees\v1.7.5-tropicana-bootstrap-fix
```
